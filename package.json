{
  "name": "custom-copilot-agent",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Copilot custom agent for managing quotes in Daily Dose — CLI extension scaffold (.github/extensions/quotes) plus programmatic harness (harness/).",
  "type": "module",
  "scripts": {
    "harness": "tsx harness/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@github/copilot-sdk": "latest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
